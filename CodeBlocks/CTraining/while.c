#include <stdio.h>

int whileTest()
{
    int count = 1;
    while(count<=5)
    {
        printf("row %d", count);
        count++;
    }
    /* printf("Enter a number please: ");
    int number;
    scanf("%d", &number);

    while(number != -1)
    {
        int square = number * number;
        printf("\nNumber of %d square: %d", number, square);
        printf("\nEnter a number please: ");
        scanf("%d", &number);
    } */
}
